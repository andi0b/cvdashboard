version: "3.8"

services:

  grafana:
    image: grafana/grafana:7.2.1
    ports:
      - "3000:3000"
    volumes:
      - "./grafana:/var/lib/grafana"

  db:
    image: postgres:13
    environment:
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - "./data:/data"
      - "./db:/db"
